services:
  checker:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: playwright-checker
    env_file:
      - ../.env
    volumes:
      - ..:/app
    working_dir: /app
    command: yarn tsx src/runner.ts
